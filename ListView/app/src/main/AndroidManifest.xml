<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.androidhive.androidlistview"
    android:versionCode="1"
    android:versionName="1.0">
    <uses-sdk android:minSdkVersion="8" />

    <application
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name">

        <activity
            android:name="com.example.user.listview.AndroidListViewActivity"
            android:label="Android List View">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <activity
            android:name="com.example.user.listview.SingleListItem"
            android:label="Single Item Selected"
            android:parentActivityName="com.example.user.listview.AndroidListViewActivity"></activity>

    </application>
</manifest>